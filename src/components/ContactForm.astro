---
const perks = [
  'Sin cuotas de entrada',
  'Formación continua gratuita',
  'Soporte dedicado',
];

const clientTypes = [
  { label: 'Autónomo', value: 'autonomo' },
  { label: 'Empresa', value: 'empresa' },
  { label: 'Particular', value: 'particular' },
];

const crmEndpoint = import.meta.env.PUBLIC_CRM_LANDING_LEADS_ENDPOINT ?? 'https://TU_DOMINIO_CRM/PTR-Sales/api/public/api_landing_leads.php?action=create';
const crmApiKey = import.meta.env.PUBLIC_NOVALUX_LANDING_API_KEY ?? '';
---

<section class="contact section" id="contacto">
  <div class="container">
    <div class="contact-panel" data-reveal>
      <div class="contact-glow" aria-hidden="true"></div>

      <div class="contact-grid">
        <div class="contact-intro">
          <h2 class="display-xl contact-title">Únete a la red líder hoy</h2>
          <p class="body-lg text-inverse contact-lead">
            Déjanos tus datos y un asesor se pondrá en contacto contigo para explicarte el programa de colaboración y darte de alta.
          </p>

          <ul class="contact-perks">
            {perks.map((perk) => (
              <li class="contact-perk">
                <span class="material-symbols-outlined contact-perk-icon" aria-hidden="true">check_circle</span>
                <span class="body-md">{perk}</span>
              </li>
            ))}
          </ul>
        </div>

        <div class="contact-form-wrap">
          <form
            class="contact-form"
            method="post"
            enctype="multipart/form-data"
            data-contact-form
            data-endpoint={crmEndpoint}
            data-api-key={crmApiKey}
            data-source="novalux"
          >
            <div class="contact-field">
              <label class="label-s contact-label" for="contact-name">Nombre completo</label>
              <input
                class="contact-input"
                id="contact-name"
                name="name"
                type="text"
                placeholder="Ej. Juan Pérez"
                required
              />
            </div>

            <div class="contact-row">
              <div class="contact-field">
                <label class="label-s contact-label" for="contact-phone">Teléfono</label>
                <input
                  class="contact-input"
                  id="contact-phone"
                  name="phone"
                  type="tel"
                  placeholder="+34 600 000 000"
                  required
                />
              </div>
              <div class="contact-field">
                <label class="label-s contact-label" for="contact-email">Email</label>
                <input
                  class="contact-input"
                  id="contact-email"
                  name="email"
                  type="email"
                  placeholder="juan@email.com"
                  required
                />
              </div>
            </div>

            <div class="contact-field">
              <label class="label-s contact-label" for="contact-client-type">Tipo de cliente</label>
              <select class="contact-input" id="contact-client-type" name="client_type" required>
                <option value="" disabled selected>Selecciona una opción</option>
                {clientTypes.map((type) => (
                  <option value={type.value}>{type.label}</option>
                ))}
              </select>
            </div>

            <div class="contact-field">
              <label class="label-s contact-label" for="contact-consumption">Consumo aproximado</label>
              <input
                class="contact-input"
                id="contact-consumption"
                name="approximate_consumption"
                type="text"
                placeholder="Ej. 150€/mes o 3.500 kWh/año"
              />
            </div>

            <fieldset class="contact-field contact-fieldset">
              <legend class="label-s contact-label">Servicios de interés</legend>
              <div class="contact-checks">
                <label class="contact-check">
                  <input type="checkbox" name="service_luz" value="1" />
                  <span>Luz</span>
                </label>
                <label class="contact-check">
                  <input type="checkbox" name="service_gas" value="1" />
                  <span>Gas</span>
                </label>
              </div>
            </fieldset>

            <div class="contact-field">
              <label class="label-s contact-label" for="contact-invoice">Subir factura</label>
              <input
                class="contact-input contact-file"
                id="contact-invoice"
                name="invoice"
                type="file"
                accept=".pdf,.jpg,.jpeg,.png,application/pdf,image/jpeg,image/png"
              />
              <p class="label-xs text-muted contact-file-hint">PDF, JPG o PNG. Máximo 10 MB.</p>
            </div>

            <button class="btn-primary contact-submit" type="submit">
              Solicitar información
            </button>

            <p class="label-s contact-status" data-form-status aria-live="polite"></p>

            <p class="label-xs text-muted contact-disclaimer">
              Al enviar aceptas nuestra <a class="label-xs" href="/privacidad">política de privacidad</a>.
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    position: relative;
    z-index: 2;
    background: var(--color-bg);
    border-radius: var(--radius-2xl);
    margin-bottom: -5rem;
  }

  .contact-panel {
    position: relative;
    overflow: hidden;
    padding: 2rem;
    border-radius: var(--radius-2xl);
    border: 1px solid rgba(255, 255, 255, 0.15);
    background: var(--color-navy);
    box-shadow: var(--shadow-lift);
  }

  .contact-glow {
    position: absolute;
    top: -6rem;
    right: -6rem;
    width: 24rem;
    height: 24rem;
    border-radius: var(--radius-full);
    background: rgba(30, 95, 140, 0.4);
    filter: blur(5rem);
    pointer-events: none;
  }

  .contact-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
  }

  .contact-title {
    margin-bottom: 1.5rem;
    color: #ffffff;
  }

  .contact-lead {
    margin-bottom: 2rem;
  }

  .contact-perks {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .contact-perk {
    display: flex;
    align-items: center;
    gap: 1rem;
    color: #ffffff;
  }

  .contact-perk-icon {
    font-family: 'Material Symbols Outlined';
    font-size: var(--size-lg);
    color: var(--color-orange);
  }

  .contact-form-wrap {
    padding: 2rem;
    border-radius: var(--radius-xl);
    background: #ffffff;
    box-shadow: var(--shadow-soft);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .contact-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .contact-field {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .contact-fieldset {
    border: none;
    padding: 0;
    margin: 0;
  }

  .contact-checks {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .contact-check {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-navy);
    cursor: pointer;
  }

  .contact-check input {
    width: 1rem;
    height: 1rem;
    accent-color: var(--color-navy);
  }

  .contact-label {
    color: var(--color-navy);
  }

  .contact-input {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-bg);
    color: var(--color-navy);
    transition: border-color 0.2s ease;
  }

  .contact-input:focus {
    outline: none;
    border-color: var(--color-navy);
  }

  .contact-file {
    padding: 0.5rem 0;
    border: none;
    background: transparent;
  }

  .contact-file-hint {
    margin: 0;
  }

  .contact-submit {
    width: 100%;
    margin-top: 0.5rem;
    padding-block: 1rem;
  }

  .contact-status {
    display: none;
    margin: 0;
    text-align: center;
  }

  .contact-status.is-visible {
    display: block;
  }

  .contact-status.is-success {
    color: #1b7f45;
  }

  .contact-status.is-error {
    color: #b42318;
  }

  .contact-disclaimer {
    text-align: center;
  }

  .contact-disclaimer a {
    color: var(--color-blue);
    text-decoration: underline;
  }

  .contact-disclaimer a:hover {
    color: var(--color-navy);
  }

  @media (min-width: 48rem) {
    .contact-panel {
      padding: 4rem;
    }

    .contact-row {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 64rem) {
    .contact-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

<script>
  const form = document.querySelector('[data-contact-form]');

  if (form instanceof HTMLFormElement) {
    const status = form.querySelector('[data-form-status]');
    const submit = form.querySelector('button[type="submit"]');
    const submitLabel = submit instanceof HTMLButtonElement ? submit.textContent : 'Enviar';

    const setStatus = (message, type) => {
      if (!(status instanceof HTMLElement)) return;
      status.textContent = message;
      status.className = `label-s contact-status is-visible is-${type}`;
    };

    form.addEventListener('submit', async (event) => {
      event.preventDefault();

      const endpoint = form.dataset.endpoint;
      const apiKey = form.dataset.apiKey;
      const source = form.dataset.source || 'novalux';

      if (!endpoint || !apiKey) {
        setStatus('El formulario no está configurado. Inténtalo más tarde.', 'error');
        return;
      }

      const data = new FormData(form);
      const body = new FormData();
      body.set('source', source);
      body.set('full_name', String(data.get('name') ?? '').trim());
      body.set('phone', String(data.get('phone') ?? '').trim());
      body.set('email', String(data.get('email') ?? '').trim());
      body.set('client_type', String(data.get('client_type') ?? '').trim());

      const consumption = String(data.get('approximate_consumption') ?? '').trim();
      if (consumption) body.set('approximate_consumption', consumption);

      if (data.get('service_luz')) body.set('service_luz', '1');
      if (data.get('service_gas')) body.set('service_gas', '1');

      const invoice = data.get('invoice');
      if (invoice instanceof File && invoice.size > 0) {
        body.set('invoice', invoice);
      }

      if (submit instanceof HTMLButtonElement) {
        submit.disabled = true;
        submit.textContent = 'Enviando...';
      }

      try {
        const response = await fetch(endpoint, {
          method: 'POST',
          headers: { 'X-API-Key': apiKey },
          body,
        });
        const result = await response.json().catch(() => null);

        if (!response.ok || result?.estado !== 1) {
          throw new Error(result?.exception || result?.message || 'No se pudo enviar el formulario');
        }

        form.reset();
        setStatus('Solicitud enviada correctamente. Te contactaremos en breve.', 'success');
      } catch (error) {
        setStatus(error instanceof Error ? error.message : 'No se pudo enviar el formulario', 'error');
      } finally {
        if (submit instanceof HTMLButtonElement) {
          submit.disabled = false;
          submit.textContent = submitLabel ?? 'Solicitar información';
        }
      }
    });
  }
</script>
