---
const perks = [
  'Sin cuotas de entrada',
  'Formación continua gratuita',
  'Soporte dedicado',
];

const businessTypes = ['Pyme/empresa', 'Individuo'];
const crmEndpoint = import.meta.env.PUBLIC_CRM_LANDING_LEADS_ENDPOINT ?? 'https://TU_DOMINIO_CRM/PTR-Sales/api/public/api_landing_leads.php?action=create';
const crmApiKey = import.meta.env.PUBLIC_NOVALUX_LANDING_API_KEY ?? '';
---

<section class="contact section" id="contacto">
  <div class="container">
    <div class="contact-panel" data-reveal>
      <div class="contact-glow" aria-hidden="true"></div>

      <div class="contact-grid">
        <div class="contact-intro">
          <h2 class="display-xl contact-title">Únete a la red líder hoy</h2>
          <p class="body-lg text-inverse contact-lead">
            Déjanos tus datos y un asesor se pondrá en contacto contigo para explicarte el programa de colaboración y darte de alta.
          </p>

          <ul class="contact-perks">
            {perks.map((perk) => (
              <li class="contact-perk">
                <span class="material-symbols-outlined contact-perk-icon" aria-hidden="true">check_circle</span>
                <span class="body-md">{perk}</span>
              </li>
            ))}
          </ul>
        </div>

        <div class="contact-form-wrap">
          <form
            class="contact-form"
            method="post"
            data-contact-form
            data-endpoint={crmEndpoint}
            data-api-key={crmApiKey}
          >
            <div class="contact-field">
              <label class="label-s contact-label" for="contact-name">Nombre completo</label>
              <input
                class="contact-input"
                id="contact-name"
                name="name"
                type="text"
                placeholder="Ej. Juan Pérez"
                required
              />
            </div>

            <div class="contact-row">
              <div class="contact-field">
                <label class="label-s contact-label" for="contact-phone">Teléfono</label>
                <input
                  class="contact-input"
                  id="contact-phone"
                  name="phone"
                  type="tel"
                  placeholder="+34 600 000 000"
                  required
                />
              </div>
              <div class="contact-field">
                <label class="label-s contact-label" for="contact-email">Email</label>
                <input
                  class="contact-input"
                  id="contact-email"
                  name="email"
                  type="email"
                  placeholder="juan@email.com"
                  required
                />
              </div>
            </div>

            <div class="contact-field">
              <label class="label-s contact-label" for="contact-business">Tipo de negocio</label>
              <select class="contact-input" id="contact-business" name="business" required>
                {businessTypes.map((type) => (
                  <option value={type}>{type}</option>
                ))}
              </select>
            </div>

            <button class="btn-primary contact-submit" type="submit">
              Solicitar información
            </button>

            <p class="label-s contact-status" data-form-status aria-live="polite"></p>

            <p class="label-xs text-muted contact-disclaimer">
              Al enviar aceptas nuestra <a class="label-xs" href="/privacidad">política de privacidad</a>.
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    position: relative;
    z-index: 2;
    background: var(--color-bg);
    border-radius: var(--radius-2xl);
    margin-bottom: -5rem;
  }

  .contact-panel {
    position: relative;
    overflow: hidden;
    padding: 2rem;
    border-radius: var(--radius-2xl);
    border: 1px solid rgba(255, 255, 255, 0.15);
    background: var(--color-navy);
    box-shadow: var(--shadow-lift);
  }

  .contact-glow {
    position: absolute;
    top: -6rem;
    right: -6rem;
    width: 24rem;
    height: 24rem;
    border-radius: var(--radius-full);
    background: rgba(30, 95, 140, 0.4);
    filter: blur(5rem);
    pointer-events: none;
  }

  .contact-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: 4rem;
  }

  .contact-title {
    margin-bottom: 1.5rem;
    color: #ffffff;
  }

  .contact-lead {
    margin-bottom: 2rem;
  }

  .contact-perks {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .contact-perk {
    display: flex;
    align-items: center;
    gap: 1rem;
    color: #ffffff;
  }

  .contact-perk-icon {
    font-family: 'Material Symbols Outlined';
    font-size: var(--size-lg);
    color: var(--color-orange);
  }

  .contact-form-wrap {
    padding: 2rem;
    border-radius: var(--radius-xl);
    background: #ffffff;
    box-shadow: var(--shadow-soft);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .contact-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .contact-field {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .contact-label {
    color: var(--color-navy);
  }

  .contact-input {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-bg);
    color: var(--color-navy);
    transition: border-color 0.2s ease;
  }

  .contact-input:focus {
    outline: none;
    border-color: var(--color-navy);
  }

  .contact-submit {
    width: 100%;
    margin-top: 0.5rem;
    padding-block: 1rem;
  }

  .contact-status {
    display: none;
    margin: 0;
    text-align: center;
  }

  .contact-status.is-visible {
    display: block;
  }

  .contact-status.is-success {
    color: #1b7f45;
  }

  .contact-status.is-error {
    color: #b42318;
  }

  .contact-disclaimer {
    text-align: center;
  }

  .contact-disclaimer a {
    color: var(--color-blue);
    text-decoration: underline;
  }

  .contact-disclaimer a:hover {
    color: var(--color-navy);
  }

  @media (min-width: 48rem) {
    .contact-panel {
      padding: 4rem;
    }

    .contact-row {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 64rem) {
    .contact-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

<script>
  const form = document.querySelector('[data-contact-form]');

  if (form instanceof HTMLFormElement) {
    const status = form.querySelector('[data-form-status]');
    const submit = form.querySelector('button[type="submit"]');

    const setStatus = (message, type) => {
      if (!(status instanceof HTMLElement)) return;
      status.textContent = message;
      status.className = `label-s contact-status is-visible is-${type}`;
    };

    form.addEventListener('submit', async (event) => {
      event.preventDefault();

      const endpoint = form.dataset.endpoint;
      const apiKey = form.dataset.apiKey;

      if (!endpoint || !apiKey) {
        setStatus('El formulario no está configurado. Inténtalo más tarde.', 'error');
        return;
      }

      const data = new FormData(form);
      const business = String(data.get('business') ?? '');
      const body = {
        source: 'novalux',
        full_name: String(data.get('name') ?? '').trim(),
        phone: String(data.get('phone') ?? '').trim(),
        email: String(data.get('email') ?? '').trim(),
        business_type: business === 'Pyme/empresa' ? 'pyme' : 'individuo',
      };

      if (submit instanceof HTMLButtonElement) {
        submit.disabled = true;
        submit.textContent = 'Enviando...';
      }

      try {
        const response = await fetch(endpoint, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-API-Key': apiKey,
          },
          body: JSON.stringify(body),
        });
        const result = await response.json().catch(() => null);

        if (!response.ok || result?.estado !== 1) {
          throw new Error(result?.exception || result?.message || 'No se pudo enviar el formulario');
        }

        form.reset();
        setStatus('Solicitud enviada correctamente. Te contactaremos en breve.', 'success');
      } catch (error) {
        setStatus(error instanceof Error ? error.message : 'No se pudo enviar el formulario', 'error');
      } finally {
        if (submit instanceof HTMLButtonElement) {
          submit.disabled = false;
          submit.textContent = 'Solicitar información';
        }
      }
    });
  }
</script>
